<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wina.partner.perfMgmt.transStmt.repository.TransStmtRepository">
  <select id="retrieveTransStmtList" resultType="map">
    SELECT * FROM TWO_WINS_COM_WHOT_DTL
  </select>
  <select id="retrieveTransStmtDetailList" resultType="map">
    SELECT * FROM TWE_ORD_I_HOSEH
  </select>
  <select id="retrieveTransStmtExcelList" resultType="map">
    SELECT * FROM TWO_SPSL_PRO_DOC
  </select>
</mapper>

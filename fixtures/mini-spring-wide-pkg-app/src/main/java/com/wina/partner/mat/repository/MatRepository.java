package com.wina.partner.mat.repository;

import com.wina.partner.mat.domain.Mat;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MatRepository extends JpaRepository<Mat, Long> {
}

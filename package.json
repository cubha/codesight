{
  "name": "codebase-viz",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^22.0.0",
    "playwright": "^1.60.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
